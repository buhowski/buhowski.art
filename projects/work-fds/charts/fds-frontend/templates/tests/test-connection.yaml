apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fds-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "fds-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fds-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
